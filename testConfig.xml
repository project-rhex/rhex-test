<?xml version="1.0" encoding="UTF-8" ?>
<configuration>

  <baseURL>http://localhost:3000/records/1</baseURL>
  
  <!-- define an invalid URI for self-test with URI that violates RFC 2396 -->
  <invalidURL>http:foo;x|y</invalidURL>
  
  <!-- create a file property of a file that does not exist -->
  <fileNotFound>dirNotFound/fileNotFound.xml</fileNotFound>
  
  <profileDocumentFile>profiles/emptyProfile.xml</profileDocumentFile>

</configuration>